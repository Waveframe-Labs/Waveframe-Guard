Read README.md and call the Waveframe connection_status tool. Show the applied publication and each configured operation identity. Fix the incorrect addition example in README.md through repository_write (modify), keeping a valid Python doctest whose expected result is 5. Create generated/new.md through repository_write (create) with the text "Governed creation proof" followed by a newline. Do not write either file using another tool. Run Python's doctest on README.md using an available Python executable, with bytecode disabled, and inspect both files. Report the actual saved evidence run IDs and test result. Do not retry a mutation on an error or lost response. Do not delegate work or change the permissions/configuration.
