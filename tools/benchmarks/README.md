## Benchmarks

These benchmarks measure the overhead of guarded execution interception.

Results represent:
- local enforcement only
- no network dependency
- typical execution boundary latency

Use for relative performance comparison, not production SLAs.
