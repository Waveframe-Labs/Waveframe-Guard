Complete, unchanged JSON bytes from Waveframe-Labs/Waveframe-Ledger
`40e0875ee9a973254bb3a4d0c228cad4fdce2bc0`, directory
`tests/fixtures/action_policy_release_v4` (Apache-2.0).
`SHA256SUMS` records every supplied JSON artifact. The example actors and approval
timestamps are fixtures, not customer approval. Guard does not rewrite them.

Compiler reproduction uses `ae590dee058d3481e384dea850d5b7d980f533ff`
(metadata 0.5.0). The separate `action_policy_v4` directory and its original
provenance remain unchanged. New acceptance also reproduces those retained
development compiler outputs using these newer client candidates; it does not
replace their original compiler provenance or prior acceptance records.
