"""Write a human-readable index from the verified final package-set manifest."""
import json,pathlib,hashlib
root=pathlib.Path(__file__).resolve().parent
r=json.loads((root/'package-set.json').read_text())
lines=['# Guard issue #52: final wheel-set handoff','',f'Guard head: `{r["guard_head"]}`. Base: `{r["guard_base"]}` on `{r["guard_base_branch"]}`.',f'[Stacked draft PR #53]({r["pr"]}). Existing #45/#47/#49/#51 heads remain unchanged; see [stack.json](stack.json).','',f'Ledger #26: `{r["ledger_head"]}`. Compiler #8: `{r["compiler_head"]}`.', 'Guard remains 0.19.0 (proposed v0.19.0). `release_ready=false`; no merge, tag, release, package upload, deployment or activation.','', '## Cloud package selection','', 'Use [package-set.json](package-set.json), with [handoff digest](handoff.json) and [SHA256SUMS](SHA256SUMS). The recommended set is the final Ubuntu/Python 3.14 build. Download its archive from this immutable evidence commit, verify the archive digest, then extract and verify the selected package members. Do not substitute an older 0.19.0 wheel by version alone.','',f'Archive: [{r["cloud_package_selection"]["archive"]}]({r["cloud_package_selection"]["archive"]})',f'SHA-256: `{r["cloud_package_selection"]["archive_sha256"]}`.','','| Selected package | SHA-256 |','| --- | --- |']
for n,p in r['cloud_package_selection']['packages'].items():lines.append(f'| {n} | `{p["sha256"]}` |')
lines+=['','The same ZIP retains original accepted Ledger/Compiler archive/build provenance, Guard clean-build inputs and build log, current installed bytes/import paths, actual pip reports, all tests/probes/HTTP evidence, and original historical Ledger/#51 records. Those historical records are not current Guard results. Per-environment packages and hashes are explicit in the manifest; source-built dependencies have separate origins and verified runtime/resource equivalence to the accepted wheel set.','', '## Final-head CI and results','']
for run in r['ci']:lines.append(f'- [{len(run["jobs"])} passing jobs]({run["url"]}) at `{run["head"]}`.')
lines+=['','All 11 required jobs completed successfully. No queued, skipped or earlier-head job is substituted. Counts below are passed / skipped, with zero failures/errors. All **150 Guard release cases execute in every source and installed suite**.','','| Cell | Source default | Source development | Installed default | Installed development | Real Cloud cases |','| --- | --- | --- | --- | --- | --- |']
for c,v in r['cells'].items():
 counts=[f'{v["guard_suites"][s]["passed"]} / {v["guard_suites"][s]["skipped"]}' for s in ('source_default','source_development','installed_default','installed_development')]
 lines.append('| '+ ' | '.join([c,*counts,'23 / 23'])+' |')
lines+=['','In every cell, the unchanged packaged Ledger suites pass **678 / 44** default and **722 / 0** development. All 70 release cases, all 44 development cases, every optional integration, the mediated v3 example, release/development package probes, and all 56 catalog-3 execution cases are required. The Guard-entry upgrade repeats execution probes after ordinary resolution from released Guard 0.18 / Ledger 0.8 / Compiler 0.4. Both old-Ledger rejection commands retain nonzero status and actual ResolutionImpossible output. Current installed provenance and pip check pass for both package entry paths.','', 'Expected skips remain explicit in JUnit and summary files: catalog-2 default opt-outs, independent released-Ledger coverage and platform/namespace restrictions. Credentials were available and all **92 real connected cases** executed.', '',f'The connected server is unchanged Cloud #147 `{r["cloud_regression_server"]}`, with original Ledger `54379d9c8044544fc1b8f32109bdfce35c1c6a05` and Compiler `3b91fcc03c804804b2ace7302f37340a787496d9` in its independent Python 3.14 environment. These are catalog-2 regression results.', '', '## Mount scope and preserved behavior','', 'All Guard runtime, tests, contracts, dependency metadata and original fixture bytes are unchanged from #51. [Source audit](source-audit.json), [Guard diff](guard-change.patch), and [Ledger runtime delta](ledger-runtime-change.patch) retain that scope. Ledger #26 changes CLI/legacy replay compatibility, not Guard filesystem enforcement. Canonical fixture outputs are reproduced by Ledger’s unchanged packaged probes; Windows sdist line endings are distinguished from canonical values.', '', 'The reviewed #51 same-device bind-mount result is retained with its original command, verified runner, raw output and build record under [retained-mount](retained-mount). Its exact tested wheel is `2b78374416635ca551ee5470fcd1e9e390d3f08141a53c91bcba7d42516b046e` at Guard `0161ef8a52e052d1bc1366cdc93ce13a9bd535ed`: one test passed, zero skips. The current Linux/Python 3.14 runtime bytes and 190 relevant build inputs match the old proof; see [mount input comparison](mount-build-input-equivalence.json) and each cell’s wrapper report. **The current wheel was not newly mount-tested.**', '', '## Failure retention and reproductions','', 'All CI command logs are retained under `ci/<run-id>/logs.zip`, with job/step outcomes and head metadata. Packaged Ledger Acceptance records retain exact command arrays, cwd, output and exit codes. Guard builds retain tracked input hashes, interpreter, command and build exit code. Source and connected pip installation reports keep their real Git/archive origins.', '', 'The initial head `004f959f6d46a40b34a10ca3a789afff8aed7d74` failed in all four combined wrappers because shallow checkouts lacked the base commit needed for ancestry verification. [Historical CI status](historical-ci-status.json) and run `34788632771` retain those failures. Fetching comparison history fixed the harness setup. Earlier green Guard jobs are retained but excluded from final results.', '', 'The original mixed-line-ending local checkout failed strict historical byte equality; [historical-local](historical-local) retains the failure, actual archives and the PowerShell exit-code limitation. A fresh local worktree then passed both producers with captured native exit codes zero; [clean-local](clean-local) retains the complete supplemental run. Neither local artifact is selected for Cloud.', '', 'Reproduction commands are in [Guard acceptance documentation](https://github.com/Waveframe-Labs/Waveframe-Guard/blob/'+r['guard_head']+'/docs/RELEASE_019_ACCEPTANCE.md). [collect_ci.py](collect_ci.py), [finalize.py](finalize.py) and [document.py](document.py) record collection/verification/index generation. Actions artifact digests are checked against actual downloaded ZIP bytes. All retained files are indexed without newline conversion.', '', '## Next owner and release limits','',f'Cloud must consume this reviewed final set and complete package, Console and production-image acceptance above #151 `{r["cloud_next_base"]}`. This task does not change or activate Cloud.', '', 'Release order remains Compiler 0.5.0 → Ledger 0.9.0 → Guard 0.19.0, ordinary index checks, then separately authorized Cloud rollout/activation. Deletion, rename and macOS remain later scope.']
(root/'README.md').write_text('\n'.join(lines)+'\n',encoding='utf-8')
hashes=['# Exact per-cell package hashes','']
for c,v in r['cells'].items():
 hashes+=['## '+c,'',f'Archive `{v["archive"]}` — `{v["archive_sha256"]}`','']
 for n,p in v['packages'].items():hashes.append(f'- `{n}`: `{p["sha256"]}`')
 hashes.append('')
(root/'PACKAGE_HASHES.md').write_text('\n'.join(hashes),encoding='utf-8')
print('Human-readable handoff generated')
