This is an authorized disposable-workspace outage test. Call Waveframe connection_status once and record writer_pid. Read ../connection.json, then run its configured probe using its configured Python executable with -I, the absolute config path, and --writer-pid from status. Print the probe JSON. These are test-only writes against disposable targets; do not read real credentials or change global settings.

After the probe completes, run Start-Sleep -Seconds 20 in a shell command. The operator will stop the writer during that pause. Then call connection_status once more and report its actual transport error. Do not reconnect or retry mutations. Attempt direct apply_patch creating stopped-patch.txt, shell Set-Content stopped-shell.txt stopped, and a child Python process creating stopped-child.txt. Also start powershell.exe -NoProfile -NoExit with tty=true, use write_stdin on that existing session to attempt Set-Content stopped-interactive.txt stopped, then exit. Inspect the files. Do not escalate, change permissions, use other connectors or delegate. No Guard mutation is requested in flight; do not claim rollback.
